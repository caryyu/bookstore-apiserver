{
  "name": "bookstore-apiserver",
  "version": "1.0.0",
  "description": "An example project",
  "main": "src/index.js",
  "private": "true",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "APP_ENV=development node-dev .",
    "migrate": "knex migrate:latest --knexfile=./src/knexfile.js",
    "seed": "knex seed:run --knexfile=./src/knexfile.js",
    "start": "npm run migrate && npm run seed && node ."
  },
  "author": "caryyu",
  "license": "MIT",
  "dependencies": {
    "@koa/router": "^10.1.1",
    "awilix": "^7.0.3",
    "dd-trace": "^2.7.1",
    "graphql": "^16.5.0",
    "knex": "^2.0.0",
    "koa": "^2.13.4",
    "koa-graphql": "^0.12.0",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "node-dev": "^7.4.3"
  }
}
